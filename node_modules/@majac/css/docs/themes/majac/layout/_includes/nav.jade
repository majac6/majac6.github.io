

nav.navbar.navbar-toggleable-md.navbar-inverse.fixed-top.bg-primary

  button(
    type="button"
    data-toggle="collapse"
    data-target="#navbarCollapse"
    aria-controls="navbarCollapse"
    aria-expanded="false"
    aria-label="Toggle navigation"
  ).navbar-toggler.navbar-toggler-right
    span.navbar-toggler-icon

  a(href="#{config.root}").navbar-brand MAJAC CSS

  div#navbarCollapse.collapse.navbar-collapse

    ul.navbar-nav.mr-auto
      each item,i in theme.menu
        - var active_class = '';
        - var href = item;
        - if(path.indexOf(i) > -1) { active_class = 'active' };
        - if(path == 'index.html' && i == 'home') { active_class = 'active'; href = ''; }

        li.nav-item
          a(
            class="nav-link #{active_class}"
            href="#{config.root}#{href}"
          ) #{i}
